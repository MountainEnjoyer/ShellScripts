## Curl command
  - Curl makes a get request to the target URL and check if wether the server is able to access the URL following HTTP request.
  - -s: silent request, it will not show progres bar
  - -w: it will display the information on terminal
  - "%{http_code}": to get the http code, for success it's 200


